<h1><%= gallery.title %></h1>

<% gallery_images_for(gallery.folder).each do |img| %>
  <div style="width: 400px; height: 300px">
    <%= image_tag(("galleries/" + "#{img}"), :style => "width: 100%; height: 100%;") %>
  </div>
<% end %>
